
**Tip:** Use `/fork` to branch the conversation. Explore a different approach without losing the original context.

**Tip:** Try the **Plan agent** to research and plan before implementing changes.



**Tip:** Use `/create-agent` to scaffold a custom agent for your workflow.

**Tip:** Use `/create-skill` to create a skill the agent can load when relevant.

**Tip:** Use `/init` to generate or update a workspace instructions file for AI coding agents.

**Tip:** Use `/create-prompt` to generate a reusable prompt file with the agent.

**Tip:** Select a code block in the editor and right-click to access more AI actions.

**Tip:** Hover a previous request and select "Restore Checkpoint" to undo changes after that point in the chat conversation.

**Tip:** Ask the agent to draw an architectural diagram or flow chart. It can render Mermaid diagrams directly in chat.

**Tip:** Customize the loading messages shown while the agent works with **thinking phrases**.

**Tip:** Something not working? Type `/troubleshoot` <question> to diagnose issues from debug logs.